{
  "name": "product-generator",
  "private": true,
  "scripts": {
    "start": "npm --prefix client install && npm --prefix client run build && npm --prefix server install && npm --prefix server run start"
  }
}
