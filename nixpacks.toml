[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm --prefix client install', 'npm --prefix server install']

[phases.build]
cmds = ['npm --prefix client run build']

[start]
cmd = 'npm --prefix server run start'
